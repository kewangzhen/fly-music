## 前端

### 组件间通信混乱

**解决方案：**

- 父子通信：使用 `props` + `emit`
- 跨层级通信：使用 `provide` / `inject`
- 复杂状态：使用 Pinia 状态管理

### 未登录用户访问需要权限的页面

**解决方案：** 使用 Vue Router 的路由守卫 `beforeEach`，在全局守卫中检查用户登录状态和 Token，未登录则重定向到登录页。

### 页面刷新后播放进度丢失

**解决方案：** 使用 Pinia 或 Vuex 全局状态管理播放器状态，包括当前歌曲、播放进度、播放状态。刷新后从最近播放历史恢复。

### 重复请求相同数据

**解决方案：** 对热门数据（如榜单、推荐歌单）做内存缓存，设置过期时间。避免用户切换页面时重复请求相同数据。

### 图片加载失败显示空白

**解决方案：** 给 `<img>` 标签添加 `@error` 事件，加载失败时使用默认占位图。同时在 CSS 中设置默认背景色。

### 表单验证规则复杂

**解决方案：** 使用 Element Plus 的 Form 验证，结合自定义校验规则。对于复杂表单，拆分为多个子表单组件，降低复杂度。

## 数据库

### 如何设计用户表支持多种角色

**问题描述：** 系统需要支持普通用户、VIP用户、管理员等多种角色，如何在用户表中体现？
**解决方案：** 添加 `role` 字段区分角色，0=普通用户，1=VIP用户，2=管理员。VIP用户的过期时间用 `vip_expire_at` 字段记录。

```sql
ALTER TABLE users ADD COLUMN role TINYINT DEFAULT 0 COMMENT '角色: 0普通用户,1VIP,2管理员';
ALTER TABLE users ADD COLUMN vip_expire_at TIMESTAMP NULL COMMENT 'VIP过期时间';
```

一首歌可能有多个歌手，一个歌手可能有多首歌，如何设计关联关系？
**解决方案：** 创建歌曲歌手关联表 `song_artists`，存储歌曲ID和歌手ID的对应关系。

```sql
CREATE TABLE song_artists (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    song_id BIGINT NOT NULL,
    artist_id BIGINT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (song_id) REFERENCES songs(id),
    FOREIGN KEY (artist_id) REFERENCES artists(id),
    UNIQUE KEY uk_song_artist (song_id, artist_id)
);
```

### 用户播放历史数据量太大查询慢

**问题描述：** 随着用户播放次数增加，播放历史表数据量快速增长，查询变慢。
**解决方案：**

- 对 `user_id` 和 `played_at` 字段建立联合索引
- 定期归档历史数据，将超过1年的数据迁移到归档表
- 查询最近播放时使用分页，只查询必要字段

```sql
CREATE INDEX idx_user_played ON play_history(user_id, played_at DESC);

-- 归档存储过程示例
CREATE PROCEDURE archive_old_history()
BEGIN
    INSERT INTO play_history_archive
    SELECT * FROM play_history
    WHERE played_at < DATE_SUB(NOW(), INTERVAL 1 YEAR);
    
    DELETE FROM play_history
    WHERE played_at < DATE_SUB(NOW(), INTERVAL 1 YEAR);
END;
```

### 热门歌曲排行榜查询效率低

**问题描述：** 按播放次数排序查询热门歌曲，需要对全表扫描。
**解决方案：**

- 使用物化视图或定时任务生成排行榜缓存表
- 预先计算热门歌曲的排名，定时刷新
- 使用 Redis 缓存热门数据

```sql
-- 创建热门歌曲缓存表
CREATE TABLE hot_rankings (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    song_id BIGINT NOT NULL,
    play_count BIGINT DEFAULT 0,
    rank_position INT DEFAULT 0,
    calculated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (song_id) REFERENCES songs(id)
);

-- 定时更新热门排行（每天凌晨执行）
CREATE EVENT daily_hot_ranking
ON SCHEDULE EVERY 1 DAY
DO
BEGIN
    INSERT INTO hot_rankings (song_id, play_count, rank_position)
    SELECT song_id, COUNT(*) as play_count,
           @row_num:=@row_num+1 as rank
    FROM play_history
    WHERE played_at > DATE_SUB(NOW(), INTERVAL 7 DAY)
    GROUP BY song_id
    ORDER BY play_count DESC
    LIMIT 100;
END;
```

### 用户敏感数据如何保护

**问题描述：** 用户密码、邮箱等敏感信息如何安全存储？
**解决方案：**

- 密码使用 BCrypt 或 MD5 加密存储
- 邮箱、手机号等敏感信息脱敏显示
- 使用数据库加密功能

```sql
-- 密码加密存储（后端处理）
-- 使用 BCrypt 加密，存储加密后的字符串

-- 脱敏显示示例
-- 邮箱: test@example.com -> t***@example.com
-- 手机: 13812345678 -> 138****5678
```

