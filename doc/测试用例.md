# Fly Music 系统测试用例文档

> 各功能模块测试用例说明

---

## 测试用例总览

| 模块 | 正确用例 | 错误用例 |
|------|---------|---------|
| 心动歌单 | 2 | 1 |
| 雷达歌单 | 2 | 1 |
| 热门推荐 | 2 | 1 |
| AI生成音乐 | 2 | 1 |
| AI曲风识别 | 2 | 1 |
| 音乐播放与创作 | 2 | 1 |
| **合计** | **12** | **6** |

---

## 1. 心动歌单模块测试用例

### 1.1 正确用例一：获取用户心动歌单（有历史行为）

**用例编号**：TC-RP-001

**测试目的**：验证用户有播放历史时，系统能够根据用户行为生成个性化心动歌单

**前置条件**：
- 用户ID为1的用户已登录系统
- 用户1有播放历史记录（至少播放过5首不同歌曲）
- 用户1有收藏记录

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/recommendation/radar/1 | 返回200状态码 |
| 2 | 验证响应数据结构 | - | 包含code: 200, data: {songs: [...]} |
| 3 | 验证歌曲数量 | - | songs数组长度为20 |
| 4 | 验证歌曲信息完整性 | - | 每首歌曲包含id, title, artist, cover字段 |

**测试数据**：
- 用户ID：1
- 请求URL：GET /api/recommendation/radar/1

**预期结果**：
- HTTP状态码：200
- 响应体：{"code": 200, "message": "success", "data": {"id": xxx, "userId": 1, "songs": [...], "interestSummary": {...}}}
- songs数组长度为20
- 歌单包含用户兴趣标签

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 1.2 正确用例二：获取用户心动歌单（无历史行为，使用默认推荐）

**用例编号**：TC-RP-002

**测试目的**：验证新用户没有任何行为记录时，系统能够返回默认推荐歌单

**前置条件**：
- 用户ID为999的用户刚注册，没有任何播放和收藏记录

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/recommendation/radar/999 | 返回200状态码 |
| 2 | 验证响应 | - | 返回默认歌单（流行、摇滚、民谣分类） |
| 3 | 验证兴趣标签 | - | interestSummary包含默认标签 |

**测试数据**：
- 用户ID：999（新建测试用户）
- 请求URL：GET /api/recommendation/radar/999

**预期结果**：
- HTTP状态码：200
- 响应包含默认推荐歌曲
- interestSummary显示默认兴趣标签

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 1.3 错误用例：用户不存在时获取心动歌单

**用例编号**：TC-RP-003

**测试目的**：验证当用户ID不存在时，系统返回正确的错误提示

**前置条件**：
- 数据库中不存在ID为99999的用户

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/recommendation/radar/99999 | 返回相应错误状态码 |
| 2 | 验证错误信息 | - | 返回错误提示信息 |

**测试数据**：
- 用户ID：99999（不存在）
- 请求URL：GET /api/recommendation/radar/99999

**预期结果**：
- HTTP状态码：404或400
- 响应体包含错误信息：{"code": 404, "message": "用户不存在"}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

## 2. 雷达歌单模块测试用例

### 2.1 正确用例一：生成新的雷达歌单

**用例编号**：TC-RD-001

**测试目的**：验证系统能够成功为用户生成雷达歌单

**前置条件**：
- 用户ID为5的用户已存在
- 用户5有至少10条播放行为记录

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起POST请求 | /api/radar/generate/5 | 返回200状态码 |
| 2 | 验证歌单生成 | - | 返回新生成的歌单信息 |
| 3 | 验证过期时间 | - | 过期时间为次日 |

**测试数据**：
- 用户ID：5
- 请求URL：POST /api/radar/generate/5

**预期结果**：
- HTTP状态码：200
- 返回歌单包含songs列表
- expiresAt字段为第二天

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 2.2 正确用例二：获取已有的未过期雷达歌单

**用例编号**：TC-RD-002

**测试目的**：验证系统能够返回缓存的未过期雷达歌单

**前置条件**：
- 用户ID为3的雷达歌单已存在且未过期

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/radar/3 | 返回200状态码 |
| 2 | 验证返回缓存数据 | - | 返回已存在的歌单 |
| 3 | 验证响应时间 | - | 响应时间小于100ms（缓存命中） |

**测试数据**：
- 用户ID：3
- 请求URL：GET /api/radar/3

**预期结果**：
- HTTP状态码：200
- 直接返回数据库中的歌单数据

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 2.3 错误用例：用户ID为空时生成雷达歌单

**用例编号**：TC-RD-003

**测试目的**：验证当用户ID为空或非法时，系统返回错误

**前置条件**：
- 无

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起POST请求 | /api/radar/generate/null | 返回400错误 |
| 2 | 验证错误信息 | - | 返回参数错误提示 |

**测试数据**：
- 用户ID：null
- 请求URL：POST /api/radar/generate/

**预期结果**：
- HTTP状态码：400
- 响应：{"code": 400, "message": "用户ID不能为空"}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

## 3. 热门推荐模块测试用例

### 3.1 正确用例一：获取全局热门榜单

**用例编号**：TC-HR-001

**测试目的**：验证系统能够返回全局热门歌曲榜单

**前置条件**：
- 数据库中存在足够多的歌曲数据
- 热门榜单已生成

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/ranking/global | 返回200状态码 |
| 2 | 验证榜单数据 | - | 返回Top100歌曲列表 |
| 3 | 验证排序 | - | 按hotScore降序排列 |

**测试数据**：
- 请求URL：GET /api/ranking/global

**预期结果**：
- HTTP状态码：200
- 返回songs数组长度为100
- 第一首歌曲hotScore最高

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 3.2 正确用例二：获取分类热门榜单

**用例编号**：TC-HR-002

**测试目的**：验证系统能够返回指定分类的热门歌曲榜单

**前置条件**：
- 分类ID为1的分类存在
- 该分类下有足够多的歌曲

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/ranking/category/1 | 返回200状态码 |
| 2 | 验证分类 | - | 返回该分类的歌曲 |
| 3 | 验证歌曲数量 | - | 返回最多100首 |

**测试数据**：
- 分类ID：1
- 请求URL：GET /api/ranking/category/1

**预期结果**：
- HTTP状态码：200
- 返回该分类下的热门歌曲

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 3.3 错误用例：获取不存在的分类榜单

**用例编号**：TC-HR-003

**测试目的**：验证当分类ID不存在时，系统返回正确的错误

**前置条件**：
- 数据库中不存在ID为999的分类

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/ranking/category/999 | 返回相应错误 |
| 2 | 验证错误信息 | - | 返回分类不存在提示 |

**测试数据**：
- 分类ID：999
- 请求URL：GET /api/ranking/category/999

**预期结果**：
- HTTP状态码：404
- 响应：{"code": 404, "message": "分类不存在"}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

## 4. AI生成音乐模块测试用例

### 4.1 正确用例一：成功生成AI音乐

**用例编号**：TC-AI-001

**测试目的**：验证系统能够成功接受AI音乐生成请求并返回任务ID

**前置条件**：
- 用户ID为1的用户已登录
- AI音乐生成服务正常运行
- 网络能够访问外部AI服务

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起POST请求 | /api/ai/generate | 返回202状态码 |
| 2 | 验证任务ID | - | 返回生成记录ID |
| 3 | 验证初始状态 | - | status为0（生成中） |

**测试数据**：
```json
{
  "userId": 1,
  "prompt": "欢快的流行音乐",
  "genre": "pop",
  "mood": "happy",
  "duration": 30
}
```

**预期结果**：
- HTTP状态码：202（Accepted）
- 返回：{"code": 200, "data": {"id": xxx, "status": 0}}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 4.2 正确用例二：查询AI音乐生成状态

**用例编号**：TC-AI-002

**测试目的**：验证能够查询AI音乐生成的最新状态

**前置条件**：
- 存在一条生成记录（ID为1）

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/ai/generate/1 | 返回200状态码 |
| 2 | 验证状态 | - | 返回当前生成状态 |
| 3 | 验证数据完整性 | - | 包含prompt、genre等字段 |

**测试数据**：
- 记录ID：1
- 请求URL：GET /api/ai/generate/1

**预期结果**：
- HTTP状态码：200
- 返回生成记录详情

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 4.3 错误用例：用户不存在时生成AI音乐

**用例编号**：TC-AI-003

**测试目的**：验证当用户ID不存在时，系统返回错误

**前置条件**：
- 用户ID为99999不存在

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起POST请求 | /api/ai/generate | 返回错误 |
| 2 | 验证错误信息 | - | 返回用户不存在提示 |

**测试数据**：
```json
{
  "userId": 99999,
  "prompt": "测试音乐",
  "genre": "pop"
}
```

**预期结果**：
- HTTP状态码：400或404
- 响应：{"code": 400, "message": "用户不存在"}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

## 5. AI曲风识别模块测试用例

### 5.1 正确用例一：上传音频文件识别曲风

**用例编号**：TC-GR-001

**测试目的**：验证系统能够上传音频文件并返回曲风识别结果

**前置条件**：
- 准备一个mp3格式的音频文件（小于10MB）
- AI曲风识别服务正常运行

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起POST请求 | /api/genre/recognize | 返回200状态码 |
| 2 | 验证识别结果 | - | 返回曲风和置信度 |
| 3 | 验证候选列表 | - | 返回多个曲风候选 |

**测试数据**：
- 文件：test.mp3（音频文件）
- 请求方式：multipart/form-data

**预期结果**：
- HTTP状态码：200
- 返回：{"code": 200, "data": {"recognizedGenre": "rock", "confidence": 0.85, "candidates": [...]}}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 5.2 正确用例二：识别已有歌曲的曲风

**用例编号**：TC-GR-002

**测试目的**：验证能够对系统中已有歌曲进行曲风识别

**前置条件**：
- 歌曲ID为1的歌曲存在且有音频文件

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/genre/recognize/1 | 返回200状态码 |
| 2 | 验证识别结果 | - | 返回曲风识别结果 |
| 3 | 验证分类更新 | - | 歌曲categoryId被更新 |

**测试数据**：
- 歌曲ID：1
- 请求URL：GET /api/genre/recognize/1

**预期结果**：
- HTTP状态码：200
- 返回识别结果并更新歌曲分类

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 5.3 错误用例：上传不支持的文件格式

**用例编号**：TC-GR-003

**测试目的**：验证当上传不支持的文件格式时，系统返回错误

**前置条件**：
- 准备一个txt格式的文件

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起POST请求 | /api/genre/recognize | 上传txt文件 |
| 2 | 验证错误信息 | - | 返回格式不支持错误 |

**测试数据**：
- 文件：test.txt（文本文件）
- 请求方式：multipart/form-data

**预期结果**：
- HTTP状态码：400
- 响应：{"code": 400, "message": "不支持的文件格式"}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

## 6. 音乐播放与创作模块测试用例

### 6.1 正确用例一：播放歌曲

**用例编号**：TC-MP-001

**测试目的**：验证系统能够成功播放歌曲并记录播放历史

**前置条件**：
- 歌曲ID为10的歌曲存在且状态正常
- 用户ID为1的用户已登录

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/player/play/10?userId=1 | 返回200状态码 |
| 2 | 验证播放URL | - | 返回有效的播放URL |
| 3 | 验证历史记录 | - | 播放历史被记录 |

**测试数据**：
- 歌曲ID：10
- 用户ID：1
- 请求URL：GET /api/player/play/10?userId=1

**预期结果**：
- HTTP状态码：200
- 返回：{"code": 200, "data": {"songUrl": "http://...", "title": "xxx", ...}}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 6.2 正确用例二：上传新歌曲

**用例编号**：TC-MP-002

**测试目的**：验证系统能够成功上传新歌曲并保存到数据库

**前置条件**：
- 准备一个mp3格式的音频文件
- 用户ID为1的用户已登录

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起POST请求 | /api/song/upload | 返回201状态码 |
| 2 | 验证歌曲信息 | - | 返回新创建的歌曲信息 |
| 3 | 验证数据持久化 | - | 数据库中新增记录 |

**测试数据**：
```json
{
  "title": "测试歌曲",
  "artistName": "测试艺术家",
  "duration": 180,
  "categoryId": 1
}
```
- 文件：song.mp3

**预期结果**：
- HTTP状态码：201
- 返回：{"code": 201, "data": {"id": xxx, "title": "测试歌曲", ...}}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

### 6.3 错误用例：播放不存在的歌曲

**用例编号**：TC-MP-003

**测试目的**：验证当播放不存在的歌曲时，系统返回错误

**前置条件**：
- 歌曲ID为99999不存在

**测试步骤**：

| 步骤 | 操作 | 输入 | 预期结果 |
|------|------|------|----------|
| 1 | 发起GET请求 | /api/player/play/99999?userId=1 | 返回404错误 |
| 2 | 验证错误信息 | - | 返回歌曲不存在提示 |

**测试数据**：
- 歌曲ID：99999
- 用户ID：1
- 请求URL：GET /api/player/play/99999?userId=1

**预期结果**：
- HTTP状态码：404
- 响应：{"code": 404, "message": "歌曲不存在"}

**实际结果**：
- [待执行测试后填写]

**测试结论**：
- [ ] 通过
- [ ] 未通过

---

## 附录：测试用例执行记录

| 用例编号 | 用例名称 | 执行日期 | 执行人 | 结果 | 备注 |
|----------|----------|----------|--------|------|------|
| TC-RP-001 | 获取用户心动歌单（有历史行为） | | | | |
| TC-RP-002 | 获取用户心动歌单（默认推荐） | | | | |
| TC-RP-003 | 用户不存在时获取心动歌单 | | | | |
| TC-RD-001 | 生成新的雷达歌单 | | | | |
| TC-RD-002 | 获取已有的未过期雷达歌单 | | | | |
| TC-RD-003 | 用户ID为空时生成雷达歌单 | | | | |
| TC-HR-001 | 获取全局热门榜单 | | | | |
| TC-HR-002 | 获取分类热门榜单 | | | | |
| TC-HR-003 | 获取不存在的分类榜单 | | | | |
| TC-AI-001 | 成功生成AI音乐 | | | | |
| TC-AI-002 | 查询AI音乐生成状态 | | | | |
| TC-AI-003 | 用户不存在时生成AI音乐 | | | | |
| TC-GR-001 | 上传音频文件识别曲风 | | | | |
| TC-GR-002 | 识别已有歌曲的曲风 | | | | |
| TC-GR-003 | 上传不支持的文件格式 | | | | |
| TC-MP-001 | 播放歌曲 | | | | |
| TC-MP-002 | 上传新歌曲 | | | | |
| TC-MP-003 | 播放不存在的歌曲 | | | | |

---

**文档版本**：1.0
**创建日期**：2026-04-12
